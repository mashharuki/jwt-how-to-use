Project commands (from package.json/README):
- Install deps: yarn
- Start React dev server: yarn start
- Build: yarn build
- Run tests: yarn test
- Start JWT server: yarn server

Common utility commands (Darwin/macOS):
- git status, git diff
- ls, cd, pwd
- rg (ripgrep) for search; find if rg unavailable
