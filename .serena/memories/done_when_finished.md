When completing a task:
- Ensure changes align with CRA + TS strict settings
- Run targeted tests if applicable: yarn test
- If build-affecting changes, verify build: yarn build
- If server-side changes, start JWT server: yarn server and sanity-check endpoints
